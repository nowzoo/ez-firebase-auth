import { ProviderTitleDirective } from './provider-title.directive';

describe('ProviderTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new ProviderTitleDirective();
    expect(directive).toBeTruthy();
  });
});
