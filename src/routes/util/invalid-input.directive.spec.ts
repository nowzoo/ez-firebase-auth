import { InvalidInputDirective } from './invalid-input.directive';

describe('InvalidInputDirective', () => {
  it('should create an instance', () => {
    const directive = new InvalidInputDirective();
    expect(directive).toBeTruthy();
  });
});
